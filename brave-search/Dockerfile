# Generated by Gemini
# Base image
FROM node:20-slim

# Set working directory
WORKDIR /app

# Install dependencies (none needed for npx execution, but good practice to have clean env)
# We use npx to run the server directly from the npm registry
# This ensures we get the latest compatible version

# Entrypoint to run the Brave Search MCP server
# It requires BRAVE_API_KEY environment variable to be set at runtime
ENTRYPOINT ["npx", "-y", "@modelcontextprotocol/server-brave-search"]
